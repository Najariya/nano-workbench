# 6.8.24 Release Handoff

Date: June 27, 2026

## Decision

Freeze `6.8.24 - Doctor evidence copy` as the next Local AI Workbench candidate and stop this experimental thread.

The older `6.7.4` Chrome Web Store submission remains the active submitted package until review completes. Submit `6.8.24` only after that review completes or if you deliberately replace the older submission.

## What Main Contains

`main` contains the `6.8.24` candidate with:

- Current-tab reading and clearer "nothing to read" diagnostics.
- Right-click selected text and image OCR workflows.
- Visible/full-page screenshot capture with local Downloads saving.
- Long-page screenshot splitting for very tall pages.
- Screenshot OCR preparation and primary **OCR + summarize** action.
- Quick voice memos and meeting notes saved locally under Memos.
- Doctor diagnostics, safe workflow evidence, and **Copy evidence**.
- No developer-operated backend, analytics, telemetry, ads, or remote data collection.

## Package To Upload Later

The package file should be:

```text
dist/local-ai-workbench-store-v6.8.24.zip
```

Create it from `main` with:

```sh
zip -r -X dist/local-ai-workbench-store-v6.8.24.zip \
  manifest.json PRIVACY.md mic-permission.html mic-permission.js \
  fonts icons safety.js sidepanel.css background.js docparse.js \
  sidepanel.html sidepanel.js db.js md.js lib export.js
```

## Chrome Test Before Store Upload

1. Open `chrome://extensions`.
2. Turn on Developer mode.
3. Click **Load unpacked**.
4. Select `/Users/naveenagrawal/Documents/Nano Workbench`.
5. Confirm version `6.8.24`.
6. Test page summary, right-click OCR, visible screenshot, full-page screenshot, screenshot **OCR + summarize**, quick voice memo, and meeting notes.
7. Open **Settings -> Doctor / Developer mode -> Copy evidence** and confirm the copied text contains only safe workflow markers, not page text, OCR text, transcript text, prompts, or images.

## Store Timing

After `6.7.4` approval completes:

1. Download or keep a copy of the approved `6.7.4` package for records.
2. Upload `dist/local-ai-workbench-store-v6.8.24.zip` as the next package.
3. Use `STORE_LISTING_DRAFT.md` for permissions and privacy text.
4. Keep the privacy policy URL as `https://github.com/Najariya/nano-workbench/blob/main/PRIVACY.md`.
